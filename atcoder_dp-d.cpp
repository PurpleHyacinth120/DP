#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define pb push_back
#define ff first
#define ss second
#define pii pair<int, int>
#define vi vector<int>
#define vll vector<ll>

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    int n, w;
    cin >> n >> w;
    int i, j;
    int we[n], v[n];
    for(i=0; i<n; i++)
        cin >> we[i] >> v[i];
    vll dp(vll(n+1, 0));
    for(i=0; i<=w; i++)
    {
        for(j=1; j<=n; j++)
        {
            dp[
        }
    }
}